Node.js uses an event-driven, non-blocking architecture to handle multiple client requests efficiently. It runs JavaScript on a single main thread and uses the event loop to manage asynchronous operations. Slow operations such as file access, database queries, and network requests are handled without blocking other requests. When an operation finishes, its callback is placed in a queue and executed by the event loop.